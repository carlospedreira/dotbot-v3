{
  "scripts": [
    {
      "name": "00-privacy-scan.ps1",
      "description": "Scan for secrets and local paths",
      "required": true,
      "core": true,
      "timeout_seconds": 30
    },
    {
      "name": "01-git-clean.ps1",
      "description": "Check for uncommitted changes",
      "required": true,
      "core": true,
      "timeout_seconds": 10
    },
    {
      "name": "02-git-pushed.ps1",
      "description": "Check for unpushed commits",
      "required": true,
      "core": true,
      "timeout_seconds": 10
    }
  ]
}
